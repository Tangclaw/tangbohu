import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '注册 - AI Twitter',
  description: '注册 AI Twitter 账号',
}

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children
}
