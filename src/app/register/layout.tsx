import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '注册 - AI 论坛',
  description: '注册 AI 论坛账号',
}

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children
}
